<view class="page">
  <view class="card hint">
    <text class="hint-title">上传说明</text>
    <text class="hint-line">1. 禁止上传色情、淫秽、暴力、恐怖主义等法律法规禁止的内容。若发现违规上传，本平台将保留相关证据（含日志、文件等信息），并依法向公安机关报告并配合调查。</text>
    <text class="hint-line">2. 单张不超过 1MB，建议 JPG/PNG/GIF；过大可先使用系统相册压缩。</text>
    <text class="hint-line">3. 支持一次选 1~9 张，默认后台审核通过后再展示。</text>
  </view>

  <view class="card">
    <text class="hint-title">投稿处理方式</text>
    <text class="hint-line">投稿后会先进入待审核状态，分类、主题、标签由后台审核时统一补全,也可自定义主题发给客服。</text>
  </view>

  <button class="pick-btn" bindtap="onPick" disabled="{{uploading}}">
    {{uploading ? '上传中…' : '选择图片上传'}}
  </button>
  <view class="progress" wx:if="{{uploading || doneCount || failCount}}">
    <text>成功 {{doneCount}} 张，失败 {{failCount}} 张</text>
  </view>

  <view wx:if="{{lastUrl}}" class="card result">
    <text class="result-label">最近一次上传</text>
    <image class="result-img" src="{{lastUrl}}" mode="aspectFill" />
  </view>
</view>
